import Typography from "typography";

const typography = new Typography({
  baseFontSize: "20px",
  bodyFontFamily: ['Libre Franklin', 'Helvetica Neue', 'sans-serif'],
  headerFontFamily: ['Georgia', 'serif'],
});

export default typography;
